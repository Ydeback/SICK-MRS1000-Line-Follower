# File for initialization of variables for the main class

import numpy as np


# Tuple to store the values of the different flags
# The values are initialize are set in the init files where the flags are
# used in the corresponding methodsets
flags = {}
# Initialize the array to hold the position of the scan layers
pos = np.double([0, 0, 0, 0])
# Initialize the array to hold the length of the scan layers
length = np.double([0, 0, 0, 0])
# Initialize the array to hold the cable angle of the scan layers
cableangle = np.double([0, 0, 0, 0])


